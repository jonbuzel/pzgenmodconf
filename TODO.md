# TODO

- лист модов не добавляется вместе с файл-списком, а надо бы,
=> а может и не надо :)
- порядок модов не регулируется, а кажется нужно бы,
=> а может и не нужно, потому как возможно уже есть добавление зависимостей в начало, и замечено что конфиг явно перезаписывается сервером, так ещё и возможно моды сам упорядочивает перетасовывает при этом
